# Palette Journal

## 2026-02-02 - Icon-only Buttons Accessibility
**Learning:** Icon-only buttons (like Edit/Delete) are common in lists but invisible to screen readers without labels.
**Action:** Always add `aria-label` describing the action (e.g., 'Edit diary', 'Delete diary') to these buttons.
